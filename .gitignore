# OS / editor
.DS_Store
.ipynb_checkpoints/
__pycache__/
*.pyc

# Never commit large/real data (notebooks generate synthetic data inline)
*.gz
station-snapshots-2019-03.csv
20250303_Viajes_distritos.csv.gz

# Publish build artifacts — generated locally each publish run, kept off the public site.
# Course-agnostic: every course folder gets a generated <course-id>/index.md and
# <course-id>/PUBLISH-LINKS.md; ignore them for any course (one level under the repo root).
*/index.md
*/PUBLISH-LINKS.md
